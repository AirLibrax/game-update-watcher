# Python 缓存
__pycache__/
*.py[cod]

# 自测产物
_selftest_out/
selftestout/

# 打包产物
*.zip

# 临时调试脚本
bili_test.py
hg_ssr_test.py

# 临时文件（工作区 臨时目录，不进仓库）
_tmp/

# 内部设计文档（不公开）
docs/

# 本地私有配置（如有）
*.local.toml
.env

# 本地插件配置（模板为 config.example.toml，实际配置不入库）
config.toml
